@model IEnumerable<MVCCRUD.Models.Genre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.From)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.From)
        </td>
        <td>
            @Html.ActionLink("编辑", "Edit", new { id=item.GenreId }) |
            @Html.ActionLink("详细", "Details", new { id=item.GenreId }) |
            @Html.ActionLink("删除", "Delete", new { id=item.GenreId })
        </td>
    </tr>
}

</table>
